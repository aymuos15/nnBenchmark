#===============================================================================
# CCLoss Configuration for Dataset001_Cellpose (fold=-1) - DynUNet
#===============================================================================
# Inherits: fold_0.yaml → base_exp.yaml → this config
# Model: DynUNet (default), 1000 epochs, CCMetric included
#===============================================================================

base_config: base_exp.yaml

overrides:
  loss:
    type: CCLoss
    to_onehot_y: true
    sigmoid: true
    softmax: false
