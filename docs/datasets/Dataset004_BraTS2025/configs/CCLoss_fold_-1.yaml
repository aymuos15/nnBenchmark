#===============================================================================
# CCLoss Configuration for Dataset004_BraTS2025 (fold=-1) - DynUNet
#===============================================================================
# Inherits: fold_0.yaml → base_exp.yaml → this config
# Model: DynUNet (default), 500 epochs, CCMetric included
#===============================================================================

base_config: base_exp.yaml

overrides:
  loss:
    type: CCLoss
    to_onehot_y: true
    sigmoid: true
    softmax: false
